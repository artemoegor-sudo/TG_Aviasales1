# 🚀 Руководство по развертыванию на Ubuntu 24.04

Это руководство поможет вам развернуть Aviasales Telegram Bot на сервере Ubuntu 24.04.

## 📋 Требования

- Ubuntu 24.04 LTS
- Права sudo
- Интернет-соединение
- Telegram Bot Token
- Aviasales API Token

## 🔧 Автоматическое развертывание

### 1. Загрузка файлов на сервер

```bash
# Загрузите все файлы проекта на сервер
scp -r TG_Aviasales/ user@your-server:/home/ubuntu/
```

### 2. Запуск скрипта развертывания

```bash
# Подключитесь к серверу
ssh user@your-server

# Перейдите в директорию проекта
cd /home/ubuntu/TG_Aviasales

# Сделайте скрипт исполняемым
chmod +x deploy.sh

# Запустите развертывание
sudo ./deploy.sh
```

## ⚙️ Ручное развертывание

### 1. Обновление системы

```bash
sudo apt update && sudo apt upgrade -y
```

### 2. Установка зависимостей

```bash
sudo apt install -y python3 python3-pip python3-venv git curl
```

### 3. Создание виртуального окружения

```bash
cd /home/ubuntu/TG_Aviasales
python3 -m venv venv
source venv/bin/activate
```

### 4. Установка Python пакетов

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 5. Настройка конфигурации

```bash
# Создайте файл .env
cp env_example .env

# Отредактируйте файл и добавьте ваши токены
nano .env
```

Добавьте в файл `.env`:
```
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
AVIASALES_API_TOKEN=ваш_aviasales_api_token
```

### 6. Настройка systemd сервиса

```bash
# Скопируйте файл сервиса
sudo cp aviasales-bot.service /etc/systemd/system/

# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable aviasales-bot
```

## 🎮 Управление ботом

### Запуск бота

```bash
sudo systemctl start aviasales-bot
```

### Остановка бота

```bash
sudo systemctl stop aviasales-bot
```

### Перезапуск бота

```bash
sudo systemctl restart aviasales-bot
```

### Проверка статуса

```bash
sudo systemctl status aviasales-bot
```

### Просмотр логов

```bash
# Просмотр всех логов
sudo journalctl -u aviasales-bot

# Просмотр логов в реальном времени
sudo journalctl -u aviasales-bot -f

# Просмотр последних 50 строк
sudo journalctl -u aviasales-bot -n 50
```

## 📁 Структура файлов

```
/home/ubuntu/TG_Aviasales/
├── telegram_bot.py          # Основной файл бота
├── aviasales_api.py         # API модуль
├── requirements.txt         # Python зависимости
├── .env                     # Конфигурация (создается при развертывании)
├── env_example              # Пример конфигурации
├── aviasales-bot.service    # Systemd сервис
├── deploy.sh                # Скрипт автоматического развертывания
├── start_bot.sh             # Скрипт ручного запуска
├── bot.log                  # Лог файл бота
├── venv/                    # Виртуальное окружение
└── README.md                # Документация
```

## 🔍 Диагностика проблем

### Бот не запускается

1. Проверьте статус сервиса:
   ```bash
   sudo systemctl status aviasales-bot
   ```

2. Проверьте логи:
   ```bash
   sudo journalctl -u aviasales-bot -n 20
   ```

3. Проверьте файл конфигурации:
   ```bash
   cat /home/ubuntu/TG_Aviasales/.env
   ```

### Бот не отвечает на сообщения

1. Проверьте интернет-соединение:
   ```bash
   ping api.telegram.org
   ```

2. Проверьте токен бота:
   ```bash
   curl "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"
   ```

### Ошибки API

1. Проверьте Aviasales API токен
2. Проверьте лимиты API
3. Проверьте логи для деталей ошибок

## 🔄 Обновление бота

```bash
# Остановите бота
sudo systemctl stop aviasales-bot

# Обновите код
cd /home/ubuntu/TG_Aviasales
git pull

# Активируйте виртуальное окружение
source venv/bin/activate

# Обновите зависимости
pip install -r requirements.txt

# Запустите бота
sudo systemctl start aviasales-bot
```

## 🔐 Безопасность

1. **Защитите файл .env**:
   ```bash
   chmod 600 /home/ubuntu/TG_Aviasales/.env
   ```

2. **Настройте firewall**:
   ```bash
   sudo ufw enable
   sudo ufw allow ssh
   sudo ufw allow out 443
   sudo ufw allow out 80
   ```

3. **Регулярно обновляйте систему**:
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

## 📊 Мониторинг

### Проверка использования ресурсов

```bash
# CPU и память
htop

# Дисковое пространство
df -h

# Сетевые соединения
netstat -tulpn
```

### Автоматический мониторинг

Создайте cron задачу для проверки статуса:

```bash
# Откройте crontab
crontab -e

# Добавьте строку для проверки каждые 5 минут
*/5 * * * * systemctl is-active --quiet aviasales-bot || systemctl restart aviasales-bot
```

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи бота
2. Убедитесь, что все токены корректны
3. Проверьте интернет-соединение
4. Перезапустите сервис

## 📈 Производительность

Для оптимальной работы рекомендуется:

- **RAM**: минимум 512MB, рекомендуется 1GB
- **CPU**: 1 ядро
- **Диск**: минимум 2GB свободного места
- **Сеть**: стабильное интернет-соединение

---

**Удачного развертывания! 🎉** 